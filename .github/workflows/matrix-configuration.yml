name: Strategy Matrix Configuration

on: push

jobs:
    deploy_on_ubuntu:
        runs-on: ubuntu-latest
        steps:
            - name: Echo docker details
              run: docker info

            - name: Run Image
              run: docker run hello-world

    deploy_on_windows:
        runs-on: windows-latest
        steps:
            - name: Echo docker details
              run: docker info

            - name: Run Image
              run: docker run hello-world